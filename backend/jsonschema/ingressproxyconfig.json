{
  "$extends": [],
  "$friendlyName": "Ingress Proxy Config",
  "$friendlyNamePlural": "Ingress Proxy Configs",
  "$modelName": "IngressProxyConfig",
  "$modelPackage": "lain",
  "$modelResourceName": "ingressproxyconfigs",
  "$modelRestName": "ingressproxyconfig",
  "additionalProperties": false,
  "description": "Represents the proxy configuration for an ingress listener of an application component.",
  "properties": {
    "defaultPolicyAccessDeniedMessage": {
      "$friendlyName": "Default Policy Access Denied Message",
      "description": "The message that is sent if the access is denied by the default policy deny action.",
      "title": "defaultPolicyAccessDeniedMessage",
      "type": [
        "string",
        "null"
      ]
    },
    "defaultPolicyAction": {
      "$defaultValue": "Allow",
      "$friendlyName": "Default Policy Action",
      "description": "The default action to take for connections ingressing to the proxy of this workload that do not match any of the policies.",
      "enum": [
        "Allow",
        "Deny"
      ],
      "title": "defaultPolicyAction"
    },
    "dialTLS": {
      "$defaultValue": "Preserve",
      "$friendlyName": "Dial TLS",
      "description": "This option controls the dialing behaviour of the apex to the origin server. Should be explicitly set to enabled if the origin expects TLS connections. And vice versa it should be set to disabled if the origin expects connections in plaintext. By default this setting is set to preserve which means that the apex is going to dial using whatever mode it terminated its own connection with: if the apex received a TLS connection, it will dial TLS, if it received a plaintext connection, then it will dial plaintext. The apex receiving behaviour can be controlled with the listenTLS option. As a last resort this setting can be set to auto detect which means that the apex is going to probe the origin server to determine if it needs to dial using TLS or not. Note that this is potentially sending a TLS Client Hello to a plaintext HTTP server which is not expecting that. Therefore, this option is not recommended if it can be avoided.",
      "enum": [
        "Preserve",
        "AutoDetect",
        "Enabled",
        "Disabled"
      ],
      "title": "dialTLS"
    },
    "listenTLS": {
      "$defaultValue": "AutoDetect",
      "$friendlyName": "Listen TLS",
      "description": "This option controls the receiving behaviour of connection to the apex before they are going to be proxied to the origin server. To control the dialing behaviour of the apex towards the origin server use the dialTLS option. Should be explicitly set to enabled if the listener expects TLS connections. And vice versa it should be set to disabled if the listener expects connections in plaintext. By default this setting is set to auto detect which means that the listener will automatically determine if incoming connections are TLS or not: TLS connections will be accepted, as well as  plain TCP connections will be accepted, both on the configured listenPort.",
      "enum": [
        "AutoDetect",
        "Enabled",
        "Disabled"
      ],
      "title": "listenTLS"
    },
    "policies": {
      "$friendlyName": "Policies",
      "description": "The defined policies for this ingress listener.",
      "items": {
        "$ref": "ingresspolicy.json"
      },
      "title": "policies",
      "type": "array"
    }
  },
  "required": [],
  "title": "IngressProxyConfig",
  "type": "object"
}