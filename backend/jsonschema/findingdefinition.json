{
  "$extends": [
    "@sharded",
    "@identifiable",
    "@importable",
    "@namelinkable",
    "@auto-propagated"
  ],
  "$friendlyName": "Finding Definition",
  "$friendlyNamePlural": "Finding Definitions",
  "$modelName": "FindingDefinition",
  "$modelPackage": "lain",
  "$modelResourceName": "findingdefinitions",
  "$modelRestName": "findingdefinition",
  "additionalProperties": false,
  "description": "Defines a finding that can be instantiated anywhere in the Acuvity system.",
  "properties": {
    "ID": {
      "$friendlyName": "ID",
      "$readOnly": true,
      "description": "ID is the identifier of the object.",
      "title": "ID",
      "type": [
        "string",
        "null"
      ]
    },
    "description": {
      "$friendlyName": "Description",
      "description": "The description of the finding.",
      "title": "description",
      "type": [
        "string",
        "null"
      ]
    },
    "friendlyName": {
      "$friendlyName": "Friendly Name",
      "$required": true,
      "description": "Friendly name of the object.",
      "title": "friendlyName",
      "type": "string"
    },
    "identities": {
      "$friendlyName": "Identities",
      "description": "The identities that can trigger this finding.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "identities",
      "type": "array"
    },
    "importHash": {
      "$creationOnly": true,
      "$friendlyName": "ImportHash",
      "description": "The hash of the structure used to compare with new import version.",
      "title": "importHash",
      "type": [
        "string",
        "null"
      ]
    },
    "importLabel": {
      "$creationOnly": true,
      "$friendlyName": "ImportLabel",
      "description": "The user-defined import label that allows the system to group resources from the same import operation.",
      "title": "importLabel",
      "type": [
        "string",
        "null"
      ]
    },
    "name": {
      "$creationOnly": true,
      "$friendlyName": "Name",
      "description": "The internal reference name of the object. It is a sanitized version of Friendly Name if empty.",
      "pattern": "(^[a-zA-Z0-9-_]+$)?",
      "title": "name",
      "type": [
        "string",
        "null"
      ]
    },
    "namespace": {
      "$friendlyName": "Namespace",
      "$readOnly": true,
      "description": "The namespace of the object.",
      "title": "namespace",
      "type": [
        "string",
        "null"
      ]
    },
    "propagate": {
      "$defaultValue": true,
      "$friendlyName": "Propagate",
      "description": "Propagates the object to all child namespaces. This is always true.",
      "title": "propagate",
      "type": [
        "boolean",
        "null"
      ]
    },
    "resolutionIdentities": {
      "$friendlyName": "Identities",
      "description": "The identities that can resolve this finding.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "resolutionIdentities",
      "type": "array"
    },
    "script": {
      "$friendlyName": "Script",
      "description": "Script to contains the Lua functions.",
      "title": "script",
      "type": [
        "string",
        "null"
      ]
    },
    "severity": {
      "$friendlyName": "Severity",
      "$required": true,
      "description": "The severity of the finding that will be created.",
      "enum": [
        "Low",
        "Medium",
        "High",
        "Critical"
      ],
      "title": "severity"
    }
  },
  "required": [
    "friendlyName",
    "severity"
  ],
  "title": "FindingDefinition",
  "type": "object"
}