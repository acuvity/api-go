{
  "$extends": [
    "@providerbase"
  ],
  "$friendlyName": "App Component Provider",
  "$friendlyNamePlural": "App Component Providers",
  "$modelName": "AppComponentProvider",
  "$modelPackage": "lain",
  "$modelResourceName": "appcomponentproviders",
  "$modelRestName": "appcomponentprovider",
  "additionalProperties": false,
  "description": "Represents the provider configuration of an application component.",
  "properties": {
    "MCPURL": {
      "$friendlyName": "MCP URL",
      "description": "The MCP endpoint URL for MCP server providers.",
      "title": "MCPURL",
      "type": [
        "string",
        "null"
      ]
    },
    "category": {
      "$friendlyName": "Category",
      "$required": true,
      "description": "The category of the provider.",
      "enum": [
        "User",
        "App"
      ],
      "title": "category"
    },
    "description": {
      "$friendlyName": "Description",
      "description": "The description of the provider.",
      "title": "description",
      "type": [
        "string",
        "null"
      ]
    },
    "errorTransformer": {
      "$friendlyName": "Error Transformer",
      "$ref": "errortransformer.json",
      "description": "Use to transform an error before sending it back to the client.",
      "title": "errorTransformer"
    },
    "extractors": {
      "$friendlyName": "Extractors",
      "description": "List of extractors for the provider.",
      "items": {
        "$ref": "extractorref.json"
      },
      "title": "extractors",
      "type": "array"
    },
    "hosts": {
      "$friendlyName": "Hosts",
      "$required": true,
      "description": "The list of hosts that this listener expects to receive traffic for.",
      "items": {
        "type": "string"
      },
      "title": "hosts",
      "type": "array"
    },
    "injectors": {
      "$friendlyName": "Injectors",
      "description": "Defines injection point for javascript snippet.",
      "items": {
        "$ref": "injector.json"
      },
      "title": "injectors",
      "type": "array"
    },
    "lib": {
      "$friendlyName": "Lib",
      "description": "If set, this snippet of lua will available in all extractors of the provider, acting as an effective library. It must be a valid lua module. It will be available to all extractor by doing local plib = require('plib').",
      "title": "lib",
      "type": [
        "string",
        "null"
      ]
    },
    "mappers": {
      "$friendlyName": "Mappers",
      "description": "List of user mappers.",
      "items": {
        "$ref": "mapper.json"
      },
      "title": "mappers",
      "type": "array"
    },
    "modelPrefixes": {
      "$friendlyName": "Model Prefixes",
      "description": "The literal prefixes that identify this provider inside a model identifier. Third-party gateways commonly address a model as the provider they route to followed by the model name, such as 'openai/gpt-4o' or '@openai-prod/gpt-4o'. Include the separator in the entry, so 'openai/' rather than 'openai': entries are matched as a plain leading string, and the separator is what keeps 'openai/' from also matching 'openai-community/gpt2'. An entry may span several segments, so 'openrouter/openai/' and 'openrouter/' can name different providers and the longer match wins. Wildcards are not supported here; use models for those.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "modelPrefixes",
      "type": "array"
    },
    "models": {
      "$friendlyName": "Models",
      "description": "The models served by this provider, as patterns. Wildcards are supported, so 'gpt-*' covers a family. They are used to resolve a provider when a model identifier arrives with no prefix, which is what several third-party gateways send on the response side of a call. Several providers may match the same model, in which case the most specific pattern wins.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "models",
      "type": "array"
    },
    "providerType": {
      "$defaultValue": "LLM",
      "$friendlyName": "Provider Type",
      "description": "The type of the provider.",
      "enum": [
        "LLM",
        "MCPServer"
      ],
      "title": "providerType"
    },
    "tools": {
      "$friendlyName": "Tools",
      "description": "The list of available tools for MCP server providers.",
      "items": {
        "$ref": "tool.json"
      },
      "title": "tools",
      "type": "array"
    },
    "trustedCA": {
      "$friendlyName": "Trusted CA",
      "description": "If set, additionally trust the Certificate Authorities from the PEM data. This is useful when working on a custom provider using a self signed CA chain.",
      "title": "trustedCA",
      "type": [
        "string",
        "null"
      ]
    },
    "upstreamUnsecure": {
      "$friendlyName": "UpstreamUnsecure",
      "description": "If enabled, force the upstream scheme to be HTTP instead of https. Mostly use for dev purposes.",
      "title": "upstreamUnsecure",
      "type": [
        "boolean",
        "null"
      ]
    }
  },
  "required": [
    "category",
    "hosts"
  ],
  "title": "AppComponentProvider",
  "type": "object"
}