{
  "$extends": [
    "@accesspolicyaction",
    "@policytransportmode"
  ],
  "$friendlyName": "Egress Policy",
  "$friendlyNamePlural": "Egress Policies",
  "$modelName": "EgressPolicy",
  "$modelPackage": "lain",
  "$modelResourceName": "egresspolicies",
  "$modelRestName": "egresspolicy",
  "additionalProperties": false,
  "description": "Represents an egress policy of an application component.",
  "properties": {
    "MCPScopes": {
      "$friendlyName": "MCP Scopes",
      "description": "The MCP servers, and optionally the tools on those servers, that this rule applies to. A scope with no tools listed applies to every tool on that server.",
      "items": {
        "$ref": "mcpserverscope.json"
      },
      "title": "MCPScopes",
      "type": "array"
    },
    "accessDeniedMessage": {
      "$friendlyName": "Access Denied Message",
      "description": "The message that is sent if the access is denied.",
      "title": "accessDeniedMessage",
      "type": [
        "string",
        "null"
      ]
    },
    "action": {
      "$defaultValue": "Allow",
      "$friendlyName": "Action",
      "description": "Define if the provider is allowed or denied for the match expression.",
      "enum": [
        "Allow",
        "Deny",
        "Redirect"
      ],
      "title": "action"
    },
    "alertDefinition": {
      "$friendlyName": "Alert Definition",
      "description": "The definition to use for alerting when action is deny.",
      "title": "alertDefinition",
      "type": [
        "string",
        "null"
      ]
    },
    "anyGatewayAllowed": {
      "$defaultValue": false,
      "$friendlyName": "All Gateways",
      "description": "If true, all gateways can be used.",
      "title": "anyGatewayAllowed",
      "type": [
        "boolean",
        "null"
      ]
    },
    "anyToolAllowed": {
      "$defaultValue": false,
      "$friendlyName": "All Tools",
      "description": "If true, all tools can be used.",
      "title": "anyToolAllowed",
      "type": [
        "boolean",
        "null"
      ]
    },
    "appComponents": {
      "$friendlyName": "App Components",
      "description": "As the egress policy is defined in the context of an application component, the application component specifies destination of the egress connection. The value is in the form of project:app/component, where the project: is optional. If this part is missing, it implies the app/component is in the same project where the app/component is defined.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "appComponents",
      "type": "array"
    },
    "contentPolicies": {
      "$friendlyName": "Content Policies",
      "description": "The list of content policies to apply when the user has access to the provider.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "contentPolicies",
      "type": "array"
    },
    "description": {
      "$friendlyName": "Description",
      "description": "Description of the access policy.",
      "title": "description",
      "type": [
        "string",
        "null"
      ]
    },
    "disabled": {
      "$friendlyName": "Disabled",
      "description": "If true, the policy is disabled.",
      "title": "disabled",
      "type": [
        "boolean",
        "null"
      ]
    },
    "excludedUserClaims": {
      "$friendlyName": "Excluded User Claims",
      "description": "The list of excluded user claims that this rule applies to.",
      "items": {
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "title": "excludedUserClaims",
      "type": "array"
    },
    "gateways": {
      "$friendlyName": "Gateways",
      "description": "The list of gateways that this rule applies to.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "gateways",
      "type": "array"
    },
    "minimalLogging": {
      "$friendlyName": "Minimal Logging",
      "description": "DEPRECATED: use redactContent, optionally together with redactContentBypass, instead. Those keep the log entry and remove the user data from it, rather than dropping the entry altogether. If true, the system will not log the messages that are not considered as violations.",
      "title": "minimalLogging",
      "type": [
        "boolean",
        "null"
      ]
    },
    "name": {
      "$friendlyName": "Name",
      "$required": true,
      "description": "The name of the access policy.",
      "pattern": "^[a-zA-Z0-9-_/@. ]+$",
      "title": "name",
      "type": "string"
    },
    "offbandAnalysis": {
      "$friendlyName": "Offband Analysis",
      "description": "If true, the decision is made on the extracted data without waiting for the analyzers. The content policy still runs and is still enforced, but it sees no analysis, so any moderation that depends on a detector cannot match. That applies to redactions as well: a redaction the analyzers would have found is not applied, and the data reaches the provider unredacted. redactionFailClose does not catch this, because no redaction was ever requested. The full analyzer set then runs in parallel and its result is attached to the log, which can report a stricter outcome than the one the request received, but never changes it. This trades detection coverage on the live request for latency.",
      "title": "offbandAnalysis",
      "type": [
        "boolean",
        "null"
      ]
    },
    "permissive": {
      "$friendlyName": "Permissive",
      "description": "If set, the content decision is computed and reported but not enforced: the request and the response go through untouched, and the verdict the policy would have applied is recorded on the log entry instead. Redactions are not applied either. This covers the content decision only. Access is still enforced: a request this policy denies, or redirects, is still denied or redirected.",
      "title": "permissive",
      "type": [
        "boolean",
        "null"
      ]
    },
    "policyID": {
      "$friendlyName": "Policy ID",
      "$readOnly": true,
      "description": "The Policy ID is the unique identifier for this policy.",
      "title": "policyID",
      "type": [
        "string",
        "null"
      ]
    },
    "providers": {
      "$friendlyName": "Providers",
      "description": "The list of providers that this rule applies to.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "providers",
      "type": "array"
    },
    "redactContent": {
      "$friendlyName": "Redact Content",
      "description": "If true, the system will remove all user data from the reported data, while keeping the analysis and metadata.",
      "title": "redactContent",
      "type": [
        "boolean",
        "null"
      ]
    },
    "redactContentBypass": {
      "$friendlyName": "Redact Content Bypass",
      "description": "If true, and redactContent is true, ignore redaction if there are some violations.",
      "title": "redactContentBypass",
      "type": [
        "boolean",
        "null"
      ]
    },
    "redactionFailClose": {
      "$friendlyName": "Block non-redactable content",
      "description": "If true, reject the request when the content policy requires a redaction that cannot be applied, for example sensitive data detected inside a non-text attachment such as a PDF or an image, where there is no character range to rewrite. When false (default), the request is allowed through with the redaction unapplied and the failed attempt is recorded on the round-trip. Default false.",
      "title": "redactionFailClose",
      "type": [
        "boolean",
        "null"
      ]
    },
    "tools": {
      "$friendlyName": "Tools",
      "description": "The list of tools that this rule applies to.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "tools",
      "type": "array"
    },
    "transportMode": {
      "$defaultValue": "Proxy",
      "$friendlyName": "Transport Mode",
      "$required": true,
      "description": "Specify if this policy applies to transparent proxy or gateway.",
      "enum": [
        "Proxy",
        "Gateway"
      ],
      "title": "transportMode"
    },
    "userClaims": {
      "$friendlyName": "User Claims",
      "description": "The list of user claims that this rule applies to.",
      "items": {
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "title": "userClaims",
      "type": "array"
    }
  },
  "required": [
    "name",
    "transportMode"
  ],
  "title": "EgressPolicy",
  "type": "object"
}