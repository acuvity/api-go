{
  "$extends": [
    "@anonymizationable",
    "@pipelinepasstype",
    "@requestcore"
  ],
  "$friendlyName": "ScanRequest",
  "$friendlyNamePlural": "ScanRequests",
  "$modelName": "ScanRequest",
  "$modelPackage": "proxy",
  "$modelResourceName": "scanrequests",
  "$modelRestName": "scanrequest",
  "additionalProperties": false,
  "description": "This is a scan request. Scan enforces no policy, so unlike police it can be used as a plain analyzer with no destination in mind: provider, destination and direction may all be left out.",
  "properties": {
    "analyzers": {
      "$friendlyName": "Analyzers",
      "description": "The analyzers parameter allows for customizing which analyzers should be used, overriding the default selection. Each analyzer entry can optionally include a prefix to modify its behavior:    - No prefix: Runs only the specified analyzers and any dependencies required for deeper analysis (slower but more acurate).   - '+' (enable): Activates an analyzer that is disabled by default.   - '-' (disable): Disables an analyzer that is enabled by default.   - '@' (direct execution): Runs the analyzer immediately, bypassing the deeper analysis (faster but less acurate).  An analyzers entry can be specified using:   - The analyzer name (e.g., 'Toxicity detector')   - The analyzer ID (e.g., 'en-text-toxicity-detector')   - The analyzer group (e.g., 'Detectors')   - A detector name (e.g., 'toxic')   - A detector label (e.g., 'insult')   - A detector group (e.g., 'Malcontents')  If left empty, all default analyzers will be executed.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "analyzers",
      "type": "array"
    },
    "anonymization": {
      "$defaultValue": "FixedSize",
      "$friendlyName": "Anonymization",
      "description": "How to anonymize the data. If deanonymize is true, then VariablSize is required.",
      "enum": [
        "FixedSize",
        "VariableSize"
      ],
      "title": "anonymization"
    },
    "conversationID": {
      "$friendlyName": "Conversation ID",
      "description": "Identifies the conversation this request belongs to. Apex records it on the resulting log, which is what groups the successive requests of one conversation together in the logs and in the conversation view, and it is also made available to the analyzers and to policies. Send the same value on every request of the same conversation.",
      "title": "conversationID",
      "type": [
        "string",
        "null"
      ]
    },
    "destination": {
      "$friendlyName": "Destination",
      "$ref": "requestdestination.json",
      "description": "The destination for this request. When destination app and component are set, they become the policy target and the provider field must not be set. On the police API an egress request must set either this or the provider: the policy needs a target. On the scan API both may be omitted to run a plain scan that targets nothing, in which case the app component identified by the caller's token is reported as the destination so that the request still shows up in traces.",
      "title": "destination"
    },
    "direction": {
      "$defaultValue": "Egress",
      "$friendlyName": "Direction",
      "description": "The direction of the traffic for this request, relative to the app component the caller's token identifies. Determines whether the ingress or the egress policies of that app component are evaluated.",
      "enum": [
        "Egress",
        "Ingress"
      ],
      "title": "direction"
    },
    "extractions": {
      "$friendlyName": "Extractions",
      "description": "The extractions to request.",
      "items": {
        "$ref": "extractionrequest.json"
      },
      "title": "extractions",
      "type": "array"
    },
    "keywords": {
      "$friendlyName": "Keywords",
      "description": "The keywords found during classification.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "keywords",
      "type": "array"
    },
    "messages": {
      "$friendlyName": "Messages",
      "description": "Messages to process and provide detections for. Use data in extractions for processing binary data.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "messages",
      "type": "array"
    },
    "provider": {
      "$friendlyName": "Provider",
      "description": "The name of the provider to use for policy resolutions. Must not be set when destination app and component are set. On the police API an egress request must set either this or the destination app and component. On the scan API both may be omitted to run a plain scan that targets nothing.",
      "title": "provider",
      "type": [
        "string",
        "null"
      ]
    },
    "redactContent": {
      "$friendlyName": "Redact Content",
      "description": "If true, the user data is removed from the logged roundtrip, while the analysis and all other metadata are kept. This only affects what is logged: the response of this call always carries the full content.",
      "title": "redactContent",
      "type": [
        "boolean",
        "null"
      ]
    },
    "redactContentBypass": {
      "$friendlyName": "Redact Content Bypass",
      "description": "If true, and redactContent is also true, the user data is kept in the logged roundtrip whenever the decision reports a violation, so that the content behind a denial stays available for review. It has no effect on its own.",
      "title": "redactContentBypass",
      "type": [
        "boolean",
        "null"
      ]
    },
    "redactions": {
      "$friendlyName": "Redactions",
      "description": "The redactions to perform if they are detected.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "redactions",
      "type": "array"
    },
    "source": {
      "$friendlyName": "Source",
      "$ref": "requestsource.json",
      "description": "The source of this request. Optional: on egress the source is already known from the caller's token, and on ingress it can be left out for an anonymous external caller.",
      "title": "source"
    },
    "tools": {
      "$friendlyName": "Tools",
      "additionalProperties": {
        "$ref": "tool.json"
      },
      "description": "The various tools used by the request.",
      "title": "tools",
      "type": "object"
    },
    "trace": {
      "$friendlyName": "Trace",
      "$ref": "requesttrace.json",
      "description": "The trace context this request belongs to. When it is set, Apex places the span it creates for this request inside your trace instead of starting a new one, and setting it enables tracing for this request even when the application is otherwise configured not to trace. What Apex actually recorded is reported back in the 'trace' field of the response.",
      "title": "trace"
    },
    "type": {
      "$friendlyName": "Type",
      "description": "The type of text.",
      "enum": [
        "Input",
        "Output"
      ],
      "title": "type"
    }
  },
  "required": [],
  "title": "ScanRequest",
  "type": "object"
}