{
  "$extends": [
    "@anonymizationable",
    "@pipelinepasstype",
    "@requestcore",
    "@traceable"
  ],
  "$friendlyName": "PoliceRequest",
  "$friendlyNamePlural": "PoliceRequests",
  "$modelName": "PoliceRequest",
  "$modelPackage": "proxy",
  "$modelResourceName": "policerequests",
  "$modelRestName": "policerequest",
  "additionalProperties": false,
  "description": "This is a police request.",
  "properties": {
    "annotations": {
      "$friendlyName": "Annotations",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Annotations attached to the extraction.",
      "title": "annotations",
      "type": "object"
    },
    "anonymization": {
      "$defaultValue": "FixedSize",
      "$friendlyName": "Anonymization",
      "description": "How to anonymize the data. If deanonymize is true, then VariablSize is required.",
      "enum": [
        "FixedSize",
        "VariableSize"
      ],
      "title": "anonymization"
    },
    "app": {
      "$friendlyName": "App",
      "$ref": "requestapp.json",
      "description": "The application processing information for this request. For police requests in an apps namespace, this is required when using an AppToken. For scan requests, this is optional and enhances logging with app/component context.",
      "title": "app"
    },
    "bypassHash": {
      "$friendlyName": "Bypass Hash",
      "description": "In the case of a contentPolicy that asks for a confirmation, this is the hash you must send back to bypass the block. This is only useful when a content policy has been set or is evaluated remotely.",
      "title": "bypassHash",
      "type": [
        "string",
        "null"
      ]
    },
    "destination": {
      "$friendlyName": "Destination",
      "$ref": "requestdestination.json",
      "description": "The destination for this request. When destination app and component are set, they become the policy target and the provider field must not be set.",
      "title": "destination"
    },
    "extractions": {
      "$friendlyName": "Extractions",
      "description": "The extractions to request.",
      "items": {
        "$ref": "extractionrequest.json"
      },
      "title": "extractions",
      "type": "array"
    },
    "messages": {
      "$friendlyName": "Messages",
      "description": "Messages to process and provide detections for. Use data in extractions for processing binary data.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "messages",
      "type": "array"
    },
    "model": {
      "$friendlyName": "Model",
      "description": "The model used by the request.",
      "title": "model",
      "type": [
        "string",
        "null"
      ]
    },
    "provider": {
      "$friendlyName": "Provider",
      "description": "The name of the provider to use for policy resolutions. Must not be set when destination app and component are set.",
      "title": "provider",
      "type": [
        "string",
        "null"
      ]
    },
    "tools": {
      "$friendlyName": "Tools",
      "additionalProperties": {
        "$ref": "tool.json"
      },
      "description": "The various tools used by the request.",
      "title": "tools",
      "type": "object"
    },
    "trace": {
      "$friendlyName": "Trace",
      "$ref": "traceref.json",
      "description": "References to the trace of the request.",
      "title": "trace"
    },
    "type": {
      "$friendlyName": "Type",
      "description": "The type of text.",
      "enum": [
        "Input",
        "Output"
      ],
      "title": "type"
    },
    "user": {
      "$friendlyName": "User",
      "$ref": "requestuser.json",
      "description": "The user information for this request. For police requests in an employees namespace, this represents the end user on whose behalf the request is made and is used for policy resolution via team assignment.",
      "title": "user"
    }
  },
  "required": [],
  "title": "PoliceRequest",
  "type": "object"
}