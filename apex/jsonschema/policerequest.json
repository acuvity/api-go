{
  "$extends": [
    "@anonymizationable",
    "@pipelinepasstype",
    "@requestcore"
  ],
  "$friendlyName": "PoliceRequest",
  "$friendlyNamePlural": "PoliceRequests",
  "$modelName": "PoliceRequest",
  "$modelPackage": "proxy",
  "$modelResourceName": "policerequests",
  "$modelRestName": "policerequest",
  "additionalProperties": false,
  "description": "This is a police request. Police enforces policy, so an egress request must name its target: either a provider or a destination app and component.",
  "properties": {
    "anonymization": {
      "$defaultValue": "FixedSize",
      "$friendlyName": "Anonymization",
      "description": "How to anonymize the data. If deanonymize is true, then VariablSize is required.",
      "enum": [
        "FixedSize",
        "VariableSize"
      ],
      "title": "anonymization"
    },
    "conversationID": {
      "$friendlyName": "Conversation ID",
      "description": "Identifies the conversation this request belongs to. Apex records it on the resulting log, which is what groups the successive requests of one conversation together in the logs and in the conversation view, and it is also made available to the analyzers and to policies. Send the same value on every request of the same conversation.",
      "title": "conversationID",
      "type": [
        "string",
        "null"
      ]
    },
    "destination": {
      "$friendlyName": "Destination",
      "$ref": "requestdestination.json",
      "description": "The destination for this request. When destination app and component are set, they become the policy target and the provider field must not be set. On the police API an egress request must set either this or the provider: the policy needs a target. On the scan API both may be omitted to run a plain scan that targets nothing, in which case the app component identified by the caller's token is reported as the destination so that the request still shows up in traces.",
      "title": "destination"
    },
    "direction": {
      "$defaultValue": "Egress",
      "$friendlyName": "Direction",
      "description": "The direction of the traffic for this request, relative to the app component the caller's token identifies. Determines whether the ingress or the egress policies of that app component are evaluated.",
      "enum": [
        "Egress",
        "Ingress"
      ],
      "title": "direction"
    },
    "extractions": {
      "$friendlyName": "Extractions",
      "description": "The extractions to request.",
      "items": {
        "$ref": "extractionrequest.json"
      },
      "title": "extractions",
      "type": "array"
    },
    "messages": {
      "$friendlyName": "Messages",
      "description": "Messages to process and provide detections for. Use data in extractions for processing binary data.",
      "items": {
        "type": [
          "string",
          "null"
        ]
      },
      "title": "messages",
      "type": "array"
    },
    "provider": {
      "$friendlyName": "Provider",
      "description": "The name of the provider to use for policy resolutions. Must not be set when destination app and component are set. On the police API an egress request must set either this or the destination app and component. On the scan API both may be omitted to run a plain scan that targets nothing.",
      "title": "provider",
      "type": [
        "string",
        "null"
      ]
    },
    "source": {
      "$friendlyName": "Source",
      "$ref": "requestsource.json",
      "description": "The source of this request. Optional: on egress the source is already known from the caller's token, and on ingress it can be left out for an anonymous external caller.",
      "title": "source"
    },
    "tools": {
      "$friendlyName": "Tools",
      "additionalProperties": {
        "$ref": "tool.json"
      },
      "description": "The various tools used by the request.",
      "title": "tools",
      "type": "object"
    },
    "trace": {
      "$friendlyName": "Trace",
      "$ref": "requesttrace.json",
      "description": "The trace context this request belongs to. When it is set, Apex places the span it creates for this request inside your trace instead of starting a new one, and setting it enables tracing for this request even when the application is otherwise configured not to trace. What Apex actually recorded is reported back in the 'trace' field of the response.",
      "title": "trace"
    },
    "type": {
      "$friendlyName": "Type",
      "description": "The type of text.",
      "enum": [
        "Input",
        "Output"
      ],
      "title": "type"
    }
  },
  "required": [],
  "title": "PoliceRequest",
  "type": "object"
}